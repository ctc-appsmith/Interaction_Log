SELECT
  empID,
	empLastName,
	empFirstName,
	empEmail,
	active,
	userName
FROM
  Employees
WHERE	
	active = "Yes"
ORDER BY
  {{employee_Table.sortOrder.column || 'empLastName'}} {{employee_Table.sortOrder.order !== "desc" ? "" : "DESC"}}
