SELECT
  *
FROM
  Teams
WHERE
  teamName LIKE '%{{Table7.searchText}}%'
ORDER BY
  {{Table7.sortOrder.column || 'teamID'}} {{Table7.sortOrder.order !== "desc" ? "" : "DESC"}}
LIMIT
  {{Table7.pageSize}}
OFFSET
  {{Table7.pageOffset}}