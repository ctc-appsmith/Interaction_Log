SELECT
  *
FROM
  BidSelectionHistory
WHERE appID = {{applications_tbl.selectedRow.appID}}
ORDER BY
  BidSelectionHistory.runDate DESC
LIMIT
  {{tasks_Table.pageSize}}
OFFSET
  {{tasks_Table.pageOffset}}