SELECT
  *
FROM
  manualOverrideTable
WHERE 
	manualOverrideTable.appID = {{applications_tbl.selectedRow.appID}} 
AND (

('{{CategorySlider1.value}}' = "All") OR (manualOverrideTable.changeType = '{{CategorySlider1.value}}')
	)
;