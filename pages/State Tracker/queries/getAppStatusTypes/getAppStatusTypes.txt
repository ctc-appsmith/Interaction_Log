SELECT
  *
FROM
  applicationStatusTypes

ORDER BY
  {{Table9.sortOrder.column || 'appStatusID'}} {{Table1.sortOrder.order !== "desc" ? "" : "DESC"}}
LIMIT
  {{Table9.pageSize}}
OFFSET
  {{Table9.pageOffset}}