SELECT
  *
FROM
  applicationStatusTypes

ORDER BY
  {{appStatusType_Table.sortOrder.column || 'appStatusID'}} {{appStatusType_Table.sortOrder.order !== "desc" ? "" : "DESC"}}
LIMIT
  {{appStatusType_Table.pageSize}}
OFFSET
  {{appStatusType_Table.pageOffset}}