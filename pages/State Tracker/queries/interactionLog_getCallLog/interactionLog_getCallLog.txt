SELECT * FROM interactionLog

WHERE clientID = '{{appsmith.store.clientID}}'

AND (('{{Select6.selectedOptionValue}}' != 0 ) AND (applicationID = '{{Select6.selectedOptionValue}}'))


AND archived = "No"

ORDER BY commsDate DESC

;