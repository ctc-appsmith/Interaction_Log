INSERT INTO entityStatus (
	entityStatusType,
	definition,
	lastChangedBy
	) 
	VALUES (
		'{{(Form1Copy.data || {}).Input3Copy}}',
		'{{(Form1Copy.data || {}).Input4Copy}}',
		'{{get_user.data.email}}'
		)
;